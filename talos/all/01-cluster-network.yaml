# yaml-language-server: $schema=https://raw.githubusercontent.com/siderolabs/talos/v1.13.0/pkg/machinery/config/schemas/config.schema.json
cluster:
  network:
    cni:
      name: none # Disable built-in CNI to use Cilium
    podSubnets:
      - "10.42.0.0/16"
    serviceSubnets:
      - "10.43.0.0/16"

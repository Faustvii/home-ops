---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &name review-runner
spec:
  chartRef:
    kind: OCIRepository
    name: home-ops-review-runner
  interval: 1h
  values:
    controllerServiceAccount:
      name: actions-runner-controller
      namespace: actions-runner-system
    githubConfigSecret: home-ops-review-runner-secret
    githubConfigUrl: https://github.com/Faustvii/home-ops
    maxRunners: 3
    # Serves agent-pr-review
    minRunners: 1
    template:
      metadata:
        labels:
          runner: *name
      spec:
        # This runner holds no credentials, so it has no use for an API token either.
        automountServiceAccountToken: false
        containers:
          - command: ["/home/runner/run.sh"]
            image: ghcr.io/home-operations/actions-runner:2.337.0@sha256:8b8e8326b5a08e658a0a41cfd2867afab69ca4194a152f986aa6fcd5430d09e7
            name: runner
            resources:
              limits:
                memory: 1Gi
              requests:
                cpu: 250m
                memory: 512Mi

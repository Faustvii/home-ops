---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ./base/ks.yaml

patches:
  - path: ./base/cluster-patch.yaml
    target:
      kind: Cluster
      name: postgres
      namespace: database

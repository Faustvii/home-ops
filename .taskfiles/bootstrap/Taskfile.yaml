---
version: "3"

tasks:
  talos:
    desc: Bootstrap the Talos cluster
    dir: "{{.TALOS_DIR}}"
    cmds:
      # Generate a Talos secrets bundle if one doesn't exist yet
      - "[ -f secrets.sops.yaml ] || talosctl gen secrets -o /dev/stdout | sops --filename-override talos/secrets.sops.yaml --encrypt /dev/stdin > secrets.sops.yaml"
      # Apply config to every node and bootstrap etcd on the first control plane
      - topf apply --mode auto --auto-bootstrap
      # Fetch talosconfig + kubeconfig once the cluster is up
      - topf talosconfig > talosconfig
      - until talosctl kubeconfig {{.ROOT_DIR}} --force; do sleep 10; done
    preconditions:
      - test -f {{.ROOT_DIR}}/.sops.yaml
      - test -f {{.SOPS_AGE_KEY_FILE}}
      - test -f {{.TALOS_DIR}}/topf.yaml
      - which topf talosctl sops

  apps:
    desc: Bootstrap apps into the Talos cluster
    cmd: bash {{.SCRIPTS_DIR}}/bootstrap-apps.sh
    preconditions:
      - msg: Unsupported bash version, run `brew install bash` to upgrade
        sh: '{{if eq OS "darwin"}}test -f /opt/homebrew/bin/bash || test -f /usr/local/bin/bash{{end}}'
      - test -f {{.KUBECONFIG}}
      - test -f {{.ROOT_DIR}}/.sops.yaml
      - test -f {{.SCRIPTS_DIR}}/bootstrap-apps.sh
      - test -f {{.SOPS_AGE_KEY_FILE}}
